# ListenerOutput
#events
Just a class to format outputs of Listeners.

# Listeners
#events 
Contains methods relative to the client to check if an event is triggered.

## listen_for(...)
#events #method 
`def listen_for(self, event: Event, *args, **kwargs) -> ListenerOutput:`
Generic method nesting every event types.

## ...(...)
 One method by event type.