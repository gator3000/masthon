Entry point when you execute the module like this :
```sh
python3 -m masthon
```

Just ask the user his token and his instance to send a post.

But if you call it by :
```sh
python3 -m masthon -v
```
it output the current version of your module. 