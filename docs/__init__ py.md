This file is the entry point of the module. When you import it, it will be executed.

First it import the `Client` [[Client py]] object from the sub-module `Client.py`,
After it define `__version__` to the current version of the package, and a default message sent by [[Examples#0. Get started]].